[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "adaptive-task-recovery"
version = "0.0.1"
description = "Adaptive Task Recovery: committed architecture (atr, src/) plus investigation spikes (spikes/ — not project architecture, see ai-notes/decisions.md D-006)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src", "spikes"]
