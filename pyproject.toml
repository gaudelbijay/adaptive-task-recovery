[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "atr-spikes"
version = "0.0.1"
description = "Investigation spikes for Adaptive Task Recovery (not project architecture — see ai-notes/decisions.md D-006)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["spikes"]
